<section class="container">
<%= link_to 'Home', root_path, class: "light-blue lighten-2 black-text btn" %>
<h1>Edit Teacher</h1>

<%= form_with model: @edit_teacher, local: true, url: teachers_update_path do |f|%>

<%= render partial: "shared/teachers", locals: {f: f} %>

<%= f.submit "Update"%>

<%end%>
</section>